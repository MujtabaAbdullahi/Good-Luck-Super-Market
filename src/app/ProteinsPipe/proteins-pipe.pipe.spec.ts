import { ProteinsPipePipe } from './proteins-pipe.pipe';

describe('ProteinsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProteinsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
